DATABASE_URL="postgresql://user:password@localhost:5432/claimtrail?schema=public"

# Xero OAuth 2.0 (read-only scopes only — see lib/xero/oauth.ts)
XERO_CLIENT_ID=""
XERO_CLIENT_SECRET=""
XERO_REDIRECT_URI="http://localhost:3000/api/xero/callback"
# Secret used to encrypt Xero tokens at rest (lib/xero/crypto.ts). Generate with:
# openssl rand -hex 32
XERO_TOKEN_ENCRYPTION_KEY=""
